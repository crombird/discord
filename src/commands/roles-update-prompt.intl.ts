import { defineMessage } from "@formatjs/intl";

export const commandName = defineMessage({
  id: "3N+Czn19",
  defaultMessage: "Roles: Update prompt",
  description: "The name of the Roles: Update prompt message command",
});
