import { defineMessage } from "@formatjs/intl";

export const commandName = defineMessage({
  id: "iHllVhlZ",
  defaultMessage: "Add to reading list",
  description: "The name of the Add to reading list message command",
});
