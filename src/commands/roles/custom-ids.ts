export const CUSTOM_ID_PREFIX = "roles";

export const CUSTOM_IDS = {
  // Manage ephemeral message
  SELECT_MANAGE_ROLE: `${CUSTOM_ID_PREFIX}-select-manage-role`,
  SELECT_MANAGE_CONFIG: `${CUSTOM_ID_PREFIX}-select-manage-config`,
  BUTTON_MANAGE_CONFIGURE: `${CUSTOM_ID_PREFIX}-button-manage-configure`,

  // Configure modals
  MODAL_CONFIGURE_AUTHORSHIP: `${CUSTOM_ID_PREFIX}-modal-configure-authorship`,
  MODAL_CONFIGURE_MEMBERSHIP: `${CUSTOM_ID_PREFIX}-modal-configure-membership`,

  // Prompt
  MODAL_PROMPT: `${CUSTOM_ID_PREFIX}-modal-prompt`,
  BUTTON_REQUEST_ROLES: `${CUSTOM_ID_PREFIX}-button-request-roles`,
  BUTTON_REQUEST_ALL_ROLES: `${CUSTOM_ID_PREFIX}-button-request-all`,
  SELECT_REQUEST_ROLE: `${CUSTOM_ID_PREFIX}-select-request-role`,
};
